import requests
import json

# respons = requests.get("http://127.0.0.1:5000")
# print(respons)

# username = "Moli"
# password = "1234"

# respons = requests.post(f"http://127.0.0.1:5000/login", json={'username': username, 'password': password})
# print(respons.json())

# respons = requests.post(f"http://127.0.0.1:5000/registration", json={'username': username, 'password': password, 'email': email})
# res = respons.json()
# print(res)

# respons = requests.post(f"http://127.0.0.1:5000/login", json={'username': username, 'password': password})
# res = respons.json()
# print(res)

# respons = requests.post(f"http://127.0.0.1:5000/login", json={'username': "Savamura", 'password': "Pass"})
# res2 = respons.json()
# print(res2)


# respons = requests.post(f"http://127.0.0.1:5000/users/{username}?key={res.get('key')}&option=add", json={"name": "vasa", "number": "89997776655"})
# print(respons.json())

# respons = requests.post(f"http://127.0.0.1:5000/users/{username}?key={res.get('key')}&option=add", json={"name": "zd", "number": "12512515"})
# print(respons.json())

# respons = requests.post(f"http://127.0.0.1:5000/users/{username}?key={res.get('key')}&option=show")
# print(respons.json())

# respons = requests.post(f"http://127.0.0.1:5000/users/{username}?key={res.get('key')}&option=delete", json={"name": "vasa"})
# print(respons.json())

# respons = requests.post(f"http://127.0.0.1:5000/users/{username}?key={res.get('key')}&option=show")
# print(respons.json())

# respons = requests.post(f"http://127.0.0.1:5000/logout", json={'username': username})
# print(respons.json())


# respons = requests.post(f"http://127.0.0.1:5000/session")
# print(respons.text)


